apiVersion: v1
kind: ConfigMap
metadata:
  name: auralog-config
  namespace: auralog
data:
  PORT: "4000"
  PHX_SERVER: "true"
  AURALOG_DUCKDB_PATH: "/data/auralog.duckdb"
  AURALOG_UDP_INGEST_ENABLED: "false"
